# 页面按钮验收矩阵

验证日期：2026-07-15。前端交互使用管理员/普通员工令牌和只读内存模拟 API；后端业务规则由 pytest 覆盖。真实 PostgreSQL 的 `postgres.public` 当前无业务表，因此未执行真实数据写入链路。

| 页面 | 管理员操作 | 验证结果 |
| --- | --- | --- |
| 登录 / 仪表盘 | 登录、周期切换、查看库存 | 登录与四项总数正常；真实登录受空库阻塞 |
| 员工 | 新建、编辑、禁用、启用、重置密码 | 表单及确认框正常；启用已改用 `/enable` |
| 客户 | 新建、编辑、上传入口 | 新建/编辑表单正常，累计消费和订单数可见 |
| 等级 / 会员价 | 新建、编辑、删除、设置/编辑会员价 | 会员价使用 SKU 和等级选择器，分页参数有效 |
| 商品 / SKU | 新建/编辑商品、添加/编辑 SKU、调整价格、保存会员价 | 抽屉和表单正常；价格抽屉无控制台错误 |
| 价格日志 | 查询、分页 | 操作人字段可见，无写操作 |
| 供应商 / 仓库 | 新建、编辑 | 表单正常，仓库联系人和状态映射正确 |
| 库存查询 | 查询、筛选、分页 | 列表契约和可用数量正常 |
| 库存操作 | 选择 SKU、清空、入库、出库、调拨、盘点 | 四个页签正常；提交有加载态、防重复和异常解锁 |
| 库存流水 | 查询、详情 | 详情弹窗正常，SKU/操作人字段匹配 |
| 订单 | 新建、选择 SKU、提交、详情、发货、收款、完成、取消 | 新建预览命中会员价 88 而非原价 100；取消原因必填 |
| 分类 | 新建、编辑、详情、添加/移除商品 | 表单和详情抽屉正常 |
| 规格 | 新建、编辑、详情、删除、解除绑定 | 表单、抽屉和确认框正常 |
| 品牌 | 新建、编辑 | 表单正常 |
| 403 / 404 / 500 | 返回首页 | 路由链接存在；管理员专属页对普通员工返回 403 |

## 权限结果

- 普通员工可只读访问商品、分类、规格和品牌页面，写按钮不渲染。
- 员工、等级、供应商和仓库页面由路由权限拦截，普通员工进入 403。
- 客户、库存操作和订单写入与后端 `CurrentUser` 权限一致。

## 自动化证据

- 后端：`26 passed`，并通过 `compileall app migrations`。
- 前端：`23 passed`，TypeScript、Biome 代码检查、Ant Design 专项检查和生产构建通过。
- 剩余环境项：DBX 确认 `postgres.public` 非系统表为 0；真实端到端业务链路需数据库初始化后执行。
