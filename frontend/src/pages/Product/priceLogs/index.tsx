import { PageContainer, ProTable } from '@ant-design/pro-components';
import type { ProColumns } from '@ant-design/pro-components';
import { listPriceChangeLogs } from '@/services/product';
import type { PriceChangeLogItem as Log } from '@/services/product';
export default function PriceLogs() { const columns: ProColumns<Log>[] = [{ title: '商品名称', dataIndex: 'product_name' }, { title: '条形码', dataIndex: 'barcode' }, { title: '价格类型', dataIndex: 'price_type' }, { title: '会员等级', dataIndex: 'level_name' }, { title: '原值', dataIndex: 'old_value' }, { title: '新值', dataIndex: 'new_value' }, { title: '原因', dataIndex: 'reason' }, { title: '操作人', dataIndex: 'operator_name' }, { title: '时间', dataIndex: 'created_at', valueType: 'dateTime' }]; return <PageContainer><ProTable<Log> rowKey="id" columns={columns} search={false} request={async (params) => { const res = await listPriceChangeLogs({ page: params.current, page_size: params.pageSize }); return { data: res.data?.items ?? [], total: res.data?.total ?? 0, success: res.code === 0 }; }} /></PageContainer>; }
